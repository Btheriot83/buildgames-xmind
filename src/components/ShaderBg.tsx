/** Flat board fill — Brandon hard bar: no decorative mesh/gradient chrome. */
export function ShaderBg() {
  return (
    <div
      className="shader-bg"
      aria-hidden="true"
      style={{ background: '#1e3328' }}
    />
  )
}
