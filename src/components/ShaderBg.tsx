/** Flat board + real chalk photo tooth — Brandon hard bar: no decorative mesh/gradient chrome. */
export function ShaderBg() {
  return (
    <div className="shader-bg" aria-hidden="true">
      <div className="shader-bg-fill" />
      <div className="shader-bg-tooth" />
    </div>
  )
}
