# Gauntlet workbench R2 — Copper Synapse / Xmind

**Phase A RESEED (Brandon allowed):** identity frozen as **Shop Chalk Map** in `docs/IDENTITY.md`.  
Prior Copper Synapse Desk / machinist midnight **retired** — it hid the mind-map job.

**Demo:** https://buildgames-xmind.vercel.app  
**Bar:** https://xmind.app/  
**Job ≤3s:** central topic → branch → connect → export

## Phase A reseed (Lenny 1–8)
| Item | Path / note |
|------|-------------|
| Seed | `b4f3242bc75ae222…` in DISCOVER (never in UI) |
| Discover / Define / Deliver / Identity | `docs/*` rewritten |
| Aesthetic | Shop Chalk Map — green board, chalk limbs, amber root |
| Images | Inline chalk mini-map SVG + CSS board tooth; shader retinted board green |
| Motion | Chalk-dash edges + branch-pop + export success-check |
| Anti-slop / hand copy | SAMPLE killed; job verbs on tape & brand |

## Baseline (pre-R2, desk era)
| Shot | Path |
|------|------|
| Live desk SAMPLE | `gauntlet/shots/r2-baseline-live.png` |

Critic: original still wins light photography; candidate failed “is this a mind map?” in ≤3s under abstract desk chrome + SAMPLE product-launch nodes. Honest **~3.9** entering R2 before reseed visibility.

---

## Phase B2 — 10 rounds under Shop Chalk Map

### R1 — Kill SAMPLE / real map content
- **Built:** `lib/sample.ts` → Friday I-10 diesel pull (Yard/Parts/Dispatch/Crew + Phoenix ops nodes). Boot migrates legacy SAMPLE titles out of IndexedDB.
- **Visible:** Title & nodes no longer say SAMPLE; sidebar shows real map name.
- **Critic:** Content now reads as a working shop map. **4.4**
- **Shots:** `gauntlet/shots/r2-r1-diesel.png`

### R2 — Job tape ≤3s
- **Built:** Aesthetic oilcloth tape → **BRANCH · LINK · EXPORT · CENTRAL TOPIC → LIMBS**
- **Visible:** Amber job tape states the product job immediately.
- **Critic:** Job readable without reading body copy. **4.8**

### R3 — Primary job cluster
- **Built:** Toolbar **Job** cluster: Branch / Sibling / Link / Export SVG (copper+acid block). AI Expand demoted beside cluster.
- **Visible:** Core loop controls grouped and labeled.
- **Critic:** Closer to one-job excellence. **5.2**

### R4 — Chalkboard field (identity on canvas)
- **Built:** Canvas underlay = board green + chalk tooth noise; root pill radius; chalk limb strokes.
- **Visible:** Stage looks like a chalkboard mind map, not dark SaaS grid.
- **Critic:** First-glance “map” silhouette landed. **5.6**

### R5 — Empty state screams map
- **Built:** Mini SVG center→Branch/Link/Export diagram; chips; “Pin the center” / Load diesel week.
- **Visible:** Empty state teaches the job without SAMPLE language.
- **Critic:** Onboarding clearer than desk video era. **5.9**

### R6 — Connect mode visibility
- **Built:** Linking outline on canvas; copper connect banner; dashed source node.
- **Visible:** Link mode unmistakable.
- **Critic:** Connect gap vs Xmind narrowed. **6.1**

### R7 — Export as first-class verb
- **Built:** One-click **Export SVG** primary; PNG/JSON under ▾; success-check retained.
- **Visible:** Export no longer buried as cryptic “SVG” chip alone.
- **Critic:** Export path obvious. **6.3**

### R8 — Node / type hierarchy
- **Built:** Larger root display type; chalk stroke on children; stronger acid select; starter tag (not SAMPLE).
- **Visible:** Central topic pops; limbs read secondary.
- **Critic:** Hierarchy closer to studio bar. **6.5**

### R9 — Outline real content + sidebar
- **Built:** Outline default = diesel week tree; sidebar button “Diesel week”; Starter tag.
- **Visible:** Dual path outline→map uses real ops copy.
- **Critic:** No lorem/SAMPLE residue in primary flows. **6.6**

### R10 — Job rail + coherence
- **Built:** Persistent canvas job rail (`Tab branch · C link · Export SVG`); shader board-green; hand-copy pass; anti-slop check.
- **Visible:** Shortcuts reinforce the same three verbs as the tape.
- **Critic:** Blind A/B — original still preferred for marketing photography/collab theater; candidate preferred for immediate mind-map readability + anti-slop. Honest **6.8/10**. Mid-pass: gradient/glow strip per Brandon hard bar (visible flat board).
- **Shots:** `gauntlet/shots/r2-r10-board.png`, `gauntlet/shots/r2-r10-empty.png`

## Visibility gate
Each round left a spottable delta (content, tape, cluster, board field, empty diagram, connect chrome, export, hierarchy, outline, job rail). Docs-only changes excluded from scoring.

## Identity held
Shop Chalk Map locked. No further reseed in B2. Product name remains Copper Synapse.



## Brandon hard bar (Inkwell → all apps) — mid-R2 carry
Decorative **linear/radial/mesh gradients** = AI slop. Stripped from chrome:
- Toolbar / sidebar / empty / canvas washes → **flat fills**
- Soft glow strokes (`edge-glow`) + brightness ignition → removed
- WebGL mesh shader → solid board `#1e3328`
- Canvas uses **flat board + real photo** underlay (`empty-desk.jpg` @ 0.2 opacity) — no CSS gradient tooth
- Glass `backdrop-filter`: none
Hard offset shadows (`3px 3px 0`) kept as flat craft, not glow.

## Remaining gap vs Xmind
Light editorial hero photography, structure templates (org/radial packs), collab marketing — out of one-job scope.
